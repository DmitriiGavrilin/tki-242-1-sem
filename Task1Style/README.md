# Кафедра — include/src/tests layout

This is the same subject-area solution and public API as the parent project, arranged in the `include`, `src`, and `tests` layout used by Task 1. The console remains interactive and every public function is covered by focused GoogleTest cases.
